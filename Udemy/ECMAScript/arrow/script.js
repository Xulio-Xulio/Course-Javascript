/* let quadrado =function(x = 0) {
    return x*x
} */

/* let quadrado = (x) => {
    return x*x
} */

let quadrado = (x) => x*x

document.write(quadrado(16))

let parOuImpar = (numero) => numero % 2 === 0 ? `par` : `impar`

console.log(parOuImpar(5))